.text-center
  = image_tag @post.image.url(:medium)
= render 'form'

.text-center.edit-links
  = link_to 'Delete post', post_path(@post), method: :delete, data: { confirm: "Are"}
  |
  = link_to "Cancel", posts_path
